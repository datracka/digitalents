query PublicFreelancers($page: Int, $per: Int) {
  publicFreelancers(page: $page, per: $per) {
    currentPage
    totalPages
    items {
      id
      genericTitle
      genericAboutMe
      aboutMe
      prettyFreelancerTypes
    }
  }
}
